import { View, Text } from "react-native";
import React from "react";

const TaskCard = ({ item }) => {
  console.log(item);
  return (
    <View>
      <Text>TaskCard</Text>
    </View>
  );
};

export default TaskCard;
